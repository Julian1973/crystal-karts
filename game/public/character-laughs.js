// Add only reviewed recordings in the canonical character voice. No generic fallback.
export const CHARACTER_LAUGHS = Object.freeze({});

// Populate with approved {ouch, whoops, laugh} recording URLs for each character.
export const CHARACTER_REACTIONS=Object.freeze({});
